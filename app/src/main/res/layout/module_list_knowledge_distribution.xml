<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cl_knowledge_distribution"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="20dp">

    <Spinner
        android:id="@+id/sp_subject"
        android:layout_width="wrap_content"
        android:layout_height="30dp"
        android:layout_marginRight="20dp"
        android:gravity="right"
        app:layout_constraintRight_toLeftOf="@id/sp_knowledge" />

    <Spinner
        android:id="@+id/sp_knowledge"
        android:layout_width="150dp"
        android:layout_height="30dp"
        android:spinnerMode="dialog"
        app:layout_constraintRight_toRightOf="@id/cl_knowledge_distribution" />

    <ListView
        android:id="@+id/lv_knowledge_distribution"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@id/sp_knowledge"
        app:layout_constraintBottom_toBottomOf="@+id/cl_knowledge_distribution"/>
</android.support.constraint.ConstraintLayout>